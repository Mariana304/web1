/*1-Elabore un algoritmo que permita calcular la base de un triangulo. Como dato de entrada se tiene el area y la altura*/


let base, altura, area;


area = 180;
altura = 20;

base = (2 * area) / altura;


console.log(`El area del triangulo es ${area}
\naltura: ${altura}
\nbase: ${base}
`);